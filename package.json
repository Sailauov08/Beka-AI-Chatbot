{
  "name": "beka-ai-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "render-build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm install --prefix backend --omit=dev",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
